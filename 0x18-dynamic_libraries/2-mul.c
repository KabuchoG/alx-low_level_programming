#include "main.h"
/**
 * mul - multipliestwo integers
 * @a: first integer
 * @b: seconf integer
 * Return: returns result
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
