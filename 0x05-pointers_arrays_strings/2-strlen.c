#include "main.h"
#include "string.h"
/**
 * _strlen - displays the lengh of a string
 * @s: the string to compute length
 * Return: integer
 */
int _strlen(char *s)
{
int length = strlen(char *s);

return (length);
}
