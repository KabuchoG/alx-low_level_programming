#include "main.h"
#include <stdio.h>
/**
 * print_array - prints an array of n size
 * @n the number of elements in the array
 * @a the array name 
 * Return: void
 */
void print_array(int *a, int n)
{
	for (; a[] <= n; a[]++)
	{
	printf("%d, ", a[]);
	}
}
