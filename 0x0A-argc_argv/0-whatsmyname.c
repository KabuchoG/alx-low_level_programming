#include "main.h"
/**
 * main - prints the name of a program
 * @argc: the number of arguments
 * @argv: the arguments
 * Return: int
 */
int main(int argc, char *argv[])
{
_putchar(argv[0]);
return (0);
}
