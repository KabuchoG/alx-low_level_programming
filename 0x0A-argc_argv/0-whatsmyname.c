#include <stdio.h>
/**
 * main - prints the name of a program
 * @argc: the number of arguments
 * @argv: the arguments
 * Return: int
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
