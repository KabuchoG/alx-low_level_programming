#include "main.h"
#include <string.h>
/**
 * strcat - concatenates a string soource to destination
 * @dest: the appended string
 * @src: the string to concatenate
 * Return:  the first element of the string array appended
 */
char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);

}
