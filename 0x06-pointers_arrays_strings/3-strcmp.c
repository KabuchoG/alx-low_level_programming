#include "main.h"
#include <string.h>
/**
 * strcmp - copraes two strings s1 and s2
 * @s1: the first string 
 * @s2: the second string
 * Return: returns an int
 */
int _strcmp(char *s1, char *s2)
{
strcmp(s1, s2);
return (0);
}
