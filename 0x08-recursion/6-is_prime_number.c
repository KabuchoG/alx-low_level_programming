#include "main.h"
/**
 * is_prime_number - finds a prime number
 * @: the integer variable
 * Return: integer
 */
int is_prime_number(int n)
{
if ((n % 10) != 0)
{
return (1);
}
else
{
return (0);
}
return (0);
}
