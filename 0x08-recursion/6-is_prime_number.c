#include "main.h"
/**
 * is_prime_number - finds a prime number
 * @n: the integer variable
 * Return: integer
 */
int is_prime_number(int n)
{
if ((n % 10) != 0)
{
return (1);
}
if (n == 1 || n < 0)
{
return (0);
}
else
{
return (0);
}
return (0);
}
