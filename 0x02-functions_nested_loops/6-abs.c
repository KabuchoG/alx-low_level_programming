#include "main.h"
/**
 * abs : determines the absolute value of a number
 *
 * Return: returns a value int
 */
int _abs(int absolutval)
{
	int n;
	int begin = 0;

	if (n > 0)
	{
	absolutval = (begin + n);
	}
	else if (n < 0)
	{
	absolutval = (begin - n);
	}
	else
	{
	absolutval = 0;
	}
}
