#include "main.h"
/**
 * _abs : determines the absolute value of a number
 * @n is an integer
 *
 * Return: returns a value integer
 */
int _abs(int n)
{	
	if (n > 0)
	{
	return (n);
	}
	else if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}
