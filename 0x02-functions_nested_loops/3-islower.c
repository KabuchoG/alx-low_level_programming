#include "main.h"
/**
 * _islower prints the lower case characters
 *
 * Return: 1  when lower case and 0 othewise
 */
int _islower(int c)
{
	int l;

	if (l = 'a' || l <= 'z');
	return (1);
	if (l = 'A'|| l <= 'Z');
	return (0);
}
