#include "main.h"
#include <stdlib.h>
/**
 * create_array - creates an array of chars
 * @size: size of array
 * @c: the string of chars
 */
char *create_array(unsigned int size, char c)
{
	c [0] = 'l';
	size = sizeof(c[0]);
	if (size == 0)
	{
		return (NULL);
	}
	else
	{
		return (NULL);
	}
	return (c);
}
